<Window x:Class="FC.PairDeviceWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pair device" Width="620" Height="350" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="{StaticResource CanvasBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Pair a teammate's computer" FontSize="21" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="Paste the FC invitation code generated on the other computer. Pairing pins that device's TLS certificate and exchanges private access keys." TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,14"/>
        <TextBox x:Name="CodeBox" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="12" FontFamily="Consolas" FontSize="11"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Pair device" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Pair_Click"/>
        </StackPanel>
    </Grid>
</Window>
