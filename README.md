# FC

FC is a Windows-first, LAN-only, peer-to-peer folder synchronizer for trusted office networks.

The goal is deliberately smaller than Syncthing: pair known Windows laptops, choose local folders, share them with teammates, and keep changes synchronized in both directions without requiring a cloud service or SMB share.

> This repository is under active initial development.
